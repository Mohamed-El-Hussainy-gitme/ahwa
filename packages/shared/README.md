# Shared Package

Shared cross-package contracts and validation utilities.

This package is introduced in phase 3 so the web and API layers can converge on one
explicit language and one contract source.
