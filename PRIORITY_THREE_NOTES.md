Priority 3 changes:
- Split waiter workspace into waiter-live and waiter-catalog slices.
- Orders page now loads live session data separately from the menu catalog.
- Dashboard page now uses nav-summary instead of the heavier dashboard workspace.
- Added dedicated routes for sliced waiter workspaces to reduce payload size and rebuild cost.
